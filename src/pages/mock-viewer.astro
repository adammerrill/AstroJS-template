---
/**
 * @fileoverview Mock Viewer Page
 * @description A dedicated route for verifying the E2E Mocking Pipeline.
 * Excluded from sitemap.
 */
 import Layout from "../layouts/Layout.astro";
import MockTester from "../components/dev/MockTester.svelte";

export const prerender = false;
---

<Layout title="QA Mock Viewer" description="Internal QA Verification Page">
  <div class="container mx-auto min-h-screen flex flex-col items-center justify-center">
    <!-- client:load ensures Svelte hydrates immediately to trigger the fetch -->
    <MockTester client:load />
  </div>
</Layout>