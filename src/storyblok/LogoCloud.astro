---
import LogoCloudSvelte from './LogoCloud.svelte';
import type { LogoCloudBlok } from '@/types/generated/storyblok';

interface Props {
  blok: LogoCloudBlok;
}

const { blok } = Astro.props;
---

<LogoCloudSvelte blok={blok} client:visible />