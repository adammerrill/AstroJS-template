---
import HeroSaasSvelte from './HeroSaas.svelte';
import type { HeroSaasBlok } from '@/types/generated/storyblok';

interface Props {
  blok: HeroSaasBlok;
}

const { blok } = Astro.props;
---

<HeroSaasSvelte blok={blok} client:idle />