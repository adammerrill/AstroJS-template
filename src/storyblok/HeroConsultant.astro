---
import HeroConsultantSvelte from './HeroConsultant.svelte';
import type { HeroConsultantBlok } from '@/types/generated/storyblok';

interface Props {
  blok: HeroConsultantBlok;
}

const { blok } = Astro.props;
---

<HeroConsultantSvelte blok={blok} client:idle />