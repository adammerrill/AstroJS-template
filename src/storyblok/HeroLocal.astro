---
import HeroLocalSvelte from './HeroLocal.svelte';
import type { HeroLocalBlok } from '@/types/generated/storyblok';

interface Props {
  blok: HeroLocalBlok;
}

const { blok } = Astro.props;
---

<HeroLocalSvelte blok={blok} client:idle />